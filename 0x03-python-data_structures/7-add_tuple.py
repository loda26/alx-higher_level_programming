#!/usr/bin/python3
def def add_tuple(tuple_a=(), tuple_b=())::
    a1 = tuble_a[0] if len(tuble_a) > 0 else 0
    a2 = tuble_a[1] if len(tuble_a) > 1 else 0
    b1 = tuble_a[0] if len(tuble_a) > 0 else 0
    b2 = tuble_a[1] if len(tuble_a) > 1 else 0
    return (a1 + b1, a2 + b2)
